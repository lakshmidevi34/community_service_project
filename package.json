{
  "name": "community-service-project",
  "version": "1.0.0",
  "description": "Community Service Project - Full Stack MERN App",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --production=false --prefix frontend",
    "build-frontend": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
